"use client";

import { ArrowUpRight, Box, Plus, Search, Server } from 'lucide-react';
import { GithubIcon } from '@/components/icons/GithubIcon';
import type { Project } from '@/lib/types';
import type { ComponentType } from 'react';

export function ProjectsHome({ projects, search, loading, onSearch, onCreate, onSelect }: { projects: Project[]; search: string; loading: boolean; onSearch: (value: string) => void; onCreate: () => void; onSelect: (id: string) => void }) {
  return <div><div className="flex flex-col justify-between gap-6 sm:flex-row sm:items-end"><div><div className="mb-3 text-xs font-semibold tracking-[0.18em] text-violet-300 uppercase">Control plane</div><h1 className="text-3xl font-semibold tracking-[-0.04em] sm:text-4xl">Good to see you.</h1><p className="mt-2 text-sm text-white/40">Select a project to enter its workspace, or connect a new repository.</p></div><button onClick={onCreate} className="inline-flex h-10 items-center justify-center gap-2 rounded-lg bg-white px-4 text-sm font-semibold text-black transition hover:bg-violet-100"><Plus className="h-4 w-4" /> New project</button></div><div className="mt-10 grid gap-4 sm:grid-cols-3"><Metric icon={Box} label="Projects" value={String(projects.length)} /><Metric icon={Server} label="Runtime" value="Docker" /><Metric icon={GithubIcon} label="Source" value="GitHub" /></div><div className="mt-12 flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><h2 className="text-lg font-semibold">Projects</h2><p className="mt-1 text-xs text-white/35">Connected repositories and deployment environments.</p></div><div className="relative w-full max-w-[240px]"><Search className="pointer-events-none absolute top-1/2 left-3 h-3.5 w-3.5 -translate-y-1/2 text-white/30" /><input value={search} onChange={(event) => onSearch(event.target.value)} placeholder="Filter projects" className="h-9 w-full rounded-lg border border-white/10 bg-white/[0.04] pr-3 pl-9 text-xs text-white outline-none placeholder:text-white/25 focus:border-violet-300/50" /></div></div><div className="mt-4 overflow-hidden rounded-xl border border-white/[0.08] bg-white/[0.025]">{loading ? <div className="px-6 py-14 text-center text-xs text-white/40">Loading projects...</div> : projects.length ? projects.map((project) => <button key={project.id} onClick={() => onSelect(project.id)} className="group flex w-full items-center gap-4 border-b border-white/[0.06] px-4 py-4 text-left transition last:border-0 hover:bg-white/[0.05]"><div className="grid h-10 w-10 shrink-0 place-items-center rounded-lg bg-gradient-to-br from-violet-500/30 to-indigo-500/10 text-sm font-bold text-violet-200">{project.name.slice(0, 1).toUpperCase()}</div><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><span className="truncate text-sm font-semibold">{project.name}</span><span className="rounded-full border border-emerald-400/20 bg-emerald-400/10 px-2 py-0.5 text-[10px] font-semibold text-emerald-300">Connected</span></div><div className="mt-1 flex items-center gap-2 text-xs text-white/35"><GithubIcon className="h-3 w-3" />{project.githubFullName}<span>·</span><span>{project.defaultBranch}</span></div></div><ArrowUpRight className="h-4 w-4 text-white/20 transition group-hover:-translate-y-0.5 group-hover:translate-x-0.5 group-hover:text-white" /></button>) : <div className="px-6 py-16 text-center"><Box className="mx-auto h-6 w-6 text-white/35" /><h3 className="mt-4 text-sm font-semibold">No projects yet</h3><p className="mt-2 text-xs text-white/35">Connect a GitHub repository to create your first project.</p><button onClick={onCreate} className="mt-5 inline-flex items-center gap-2 rounded-lg border border-white/15 px-3 py-2 text-xs font-semibold hover:bg-white/10"><Plus className="h-3.5 w-3.5" /> Create project</button></div>}</div></div>;
}

function Metric({ icon: Icon, label, value }: { icon: ComponentType<{ className?: string }>; label: string; value: string }) { return <div className="rounded-xl border border-white/[0.08] bg-white/[0.025] p-4"><div className="flex items-center justify-between text-xs text-white/35"><span>{label}</span><Icon className="h-4 w-4 text-white/30" /></div><div className="mt-4 text-2xl font-semibold tracking-tight">{value}</div></div>; }
