import { KeyRound, LayoutDashboard, Settings, TerminalSquare } from 'lucide-react';
import type { DashboardView } from '@/lib/types';
const items: { id: DashboardView; label: string; icon: typeof LayoutDashboard }[] = [{ id: 'overview', label: 'Overview', icon: LayoutDashboard }, { id: 'logs', label: 'Logs', icon: TerminalSquare }, { id: 'environment', label: 'Environment', icon: KeyRound }, { id: 'settings', label: 'Settings', icon: Settings }];
export function MobileNav({ view, onView }: { view: DashboardView; onView: (view: DashboardView) => void }) { return <div className="border-b border-white/[0.07] px-5 py-3 lg:hidden"><div className="flex gap-2 overflow-x-auto">{items.map((item) => { const Icon = item.icon; return <button key={item.id} onClick={() => onView(item.id)} className={`flex shrink-0 items-center gap-2 rounded-lg px-3 py-2 text-xs ${view === item.id ? 'bg-white/10 text-white' : 'text-white/45'}`}><Icon className="h-3.5 w-3.5" />{item.label}</button>; })}</div></div>; }
